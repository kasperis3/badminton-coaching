# Copy values into the systemd unit (Environment=) or export before running gunicorn.
# Generate a secret: python3 -c "import secrets; print(secrets.token_hex(32))"

SECRET_KEY=replace-with-a-long-random-string
